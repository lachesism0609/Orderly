{
    "name": "orderly-food-ordering-app",
    "version": "1.0.0",
    "description": "Full-stack food ordering and review web application with React.js frontend and Node.js/Express backend, using Firebase for authentication and Firestore for data storage.",
    "main": "index.js",
    "scripts": {
        "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
        "start": "concurrently \"npm run server:start\" \"npm run client:start\"",
        "server:dev": "cd server && npm run dev",
        "server:start": "cd server && npm start",
        "client:dev": "cd client && npm start",
        "client:start": "cd client && npm run build && serve -s build",
        "install:all": "npm install && cd server && npm install && cd ../client && npm install",
        "build": "cd client && npm run build",
        "test": "cd client && npm test && cd ../server && npm test"
    },
    "keywords": [
        "food-ordering",
        "react",
        "nodejs",
        "express",
        "firebase",
        "firestore",
        "full-stack",
        "web-application",
        "bilingual",
        "restaurant"
    ],
    "author": "Your Name",
    "license": "MIT",
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": ">=16.0.0",
        "npm": ">=8.0.0"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/your-username/orderly-food-ordering-app.git"
    }
}